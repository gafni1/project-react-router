import { NavLink } from "react-router-dom";

function Navbaregistered() {
  return (
    <nav className="navbar">
      {/* <NavLink to="/about" className={({ isActive }) => (isActive ? 'link active' : 'link')}>users</NavLink> */}
      {/* <NavLink to="/products" className={({ isActive }) => (isActive ? 'link active' : 'link')}>todos</NavLink> */}
      {/* <NavLink to="/login" className={({ isActive }) => (isActive ? 'link active' : 'link')}>photos</NavLink> */}
      {/* <NavLink to="/login" className={({ isActive }) => (isActive ? 'link active' : 'link')}>comments</NavLink> */}
      {/* <NavLink to="/login" className={({ isActive }) => (isActive ? 'link active' : 'link')}>posts</NavLink> */}
    </nav>
  );
}

export default Navbaregistered;